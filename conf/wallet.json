{
    "keyPairs": ["!../keyfile.json:.keyPair"],
    "storage": {
        "database": {
            "driver": {
                "sqlite": "data/nodes.sqlite"
            },
            "blobDriver": {
                "sqlite": "data/blobs.sqlite"
            }
        }
    }
}
